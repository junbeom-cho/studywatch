# Product Requirement Document

<!--
쓰는 법
- 이 문서는 "무엇을, 왜"까지만 적는다. "어떻게"는 코드와 커밋에 남는다.
- 해당 없는 절은 지운다. 주말에 끝날 스크립트라면 §1·§3·§4만 남겨도 된다.
- 모르는 항목은 빈칸으로 두지 말고 `TBD`라 적거나 §6으로 내린다. 빈칸은 "안 정했다"인지 "필요 없다"인지 구분이 안 된다.
- 채우는 데 30분이 넘어가면 문서가 프로젝트보다 커진 것이다.
-->

| 항목 | 내용 |
|---|---|
| 프로젝트 | \<이름\> |
| 상태 | 구상 중 <!-- 구상 중 / 만드는 중 / 완성해서 쓰는 중 / 멈춤 --> |

---

## 1. 한 줄 요약

<!-- "무엇을 하고, 왜 필요한가"를 한 문장으로. 이 문장이 안 써지면 아직 만들 때가 아니다. -->

> (예) 매일 아침 채용 공고 사이트 다섯 곳을 긁어 새 글만 메일 한 통으로 보내주는 CLI 도구.

## 2. 왜 만드는가

<!-- 반년 뒤에 이 문서를 다시 열어볼 나를 위한 절이다. 그때 가장 먼저 사라지는 기억이 "내가 이걸 왜 시작했지"다. 쓰다가 '별로 안 불편한데?' 싶으면 안 만드는 것도 정답이다. -->

- **지금은 이렇게 한다**: \<(예) 아침마다 브라우저 탭 다섯 개를 직접 연다.\>
- **뭐가 불편한가**: \<정량화할 수 있으면 한다. "느리다"가 아니라 "매일 20분씩, 그러다 이틀 걸러 안 보게 된다".\>

## 3. 비목표

<!--
이 문서에서 제일 중요한 절이다. 개인 프로젝트는 기능이 모자라서가 아니라 자꾸 붙이다가 죽는다.
만들다 떠오른 "이것도 있으면 좋을 텐데"는 구현하지 말고 여기 적는다. 적어두면 미련이 덜 남는다.
정말 필요해지면 그때 NG 줄을 지우고 §4 표로 옮긴다. 언제 왜 마음을 바꿨는지는 git 기록이 대신 남겨 준다.
-->

- NG-1. \<(예) 웹 UI는 만들지 않는다. 터미널 출력과 메일이면 충분하다.\>
- NG-2. \<(예) 나 말고 다른 사람이 쓰는 경우는 생각하지 않는다. 로그인·권한 없음.\>
- NG-3. \<(예) 사이트 구조가 바뀌면 그때 손으로 고친다. 자동 복구는 하지 않는다.\>

## 4. 기능 요구사항

<!--
우선순위는 MVP / 다음 / 나중 세 가지면 된다. 혼자 하는 일에 4단계는 과하다.
아예 안 할 것은 표에서 지우고 §3으로 옮긴다.
수용 기준은 "돌려보고 참·거짓을 말할 수 있는 문장"으로 쓴다. 그대로 테스트 케이스가 된다.
FR 번호는 커밋 메시지(`feat(collect): FR-2 사이트별 수집 추가`)나 에이전트에게 시킬 때("FR-3부터 해줘") 쓰려고 붙인 것이다.
-->

| ID | 기능 | 우선순위 | 수용 기준 |
|---|---|---|---|
| FR-1 | \<(예) 사이트별 공고 수집\> | MVP | \<(예) 사이트 다섯 곳에서 제목·링크·마감일을 뽑아 저장한다\> |
| FR-2 | \<기능\> | \<MVP / 다음 / 나중\> | \<돌려보고 참·거짓을 말할 수 있는 문장\> |
| FR-3 | \<기능\> | \<MVP / 다음 / 나중\> | \<수용 기준\> |

- **MVP 판정**: \<(예) 손으로 실행해서 메일이 한 통 오면 끝.\>

### 4.1 \<FR-N\> \<기능명\>

<!-- 표 한 줄로 안 끝나는 기능만 이렇게 따로 펼친다. 전부 펼치면 위 표가 있을 이유가 없다. -->

- **하는 일**: \<무엇이 들어와서 무엇이 남는가\>
- **실패하면**: \<(예) 사이트 한 곳이 죽어도 나머지는 계속 수집하고 로그만 남긴다.\>

## 5. 기술

### 5.1 스택과 고른 이유

<!-- 이유가 "손에 익어서"여도 적어둔다. 나중에 갈아탈지 말지가 그 한 줄로 갈린다. 다시 찾기 귀찮았던 링크도 이 칸에 그대로 붙인다. -->

| 영역 | 선택 | 고른 이유 |
|---|---|---|
| 언어 · 런타임 | \<(예) Python 3.12 + uv\> | \<(예) 파싱 라이브러리가 손에 익다\> |
| 주요 라이브러리 | \<…\> | \<…\> |
| 저장소 | \<(예) SQLite / 파일 / 안 씀\> | \<(예) 파일 하나로 끝나고 서버가 필요 없다\> |
| 돌아가는 곳 | \<(예) 내 노트북 / 홈서버 cron / GitHub Actions\> | \<…\> |

### 5.2 데이터

<!-- 무엇을 저장하고 무엇끼리 이어지는지만 한두 줄로. 표가 셋을 넘어가면 그때 그림 하나 그려서 이 절에 붙인다. -->

- \<(예) 공고(회사, 제목, 링크, 마감일, 수집일시) 테이블 하나가 전부다.\>

### 5.3 신경 쓸 것

<!-- 개인 프로젝트에서 실제로 발목을 잡는 것만 남기고, 해당 없는 줄은 통째로 지운다. -->

- **체감 속도**: \<(예) 전체 수집이 1분 안에 끝난다. 그보다 느리면 아침에 안 돌리게 된다.\>
- **비밀정보**: \<(예) 필요한 건 메일 SMTP 비밀번호 하나. 다루는 규칙은 AGENTS.md §7.\>
- **지원 범위**: \<(예) Windows 11 + PowerShell에서만 돌면 된다.\>
- **비용**: \<(예) 월 고정비 0원. 넘어가면 접는다.\>

## 6. 미해결 질문

<!-- "이게 정해지기 전엔 코드를 못 짠다"만 적는다. 정해지면 본문으로 옮기고 여기서 지운다. 안 지우면 이 절이 곧 쓰레기통이 된다. -->

- Q-1. \<(예) 로그인이 필요한 사이트는 어떻게 통과할 것인가\> → 막히는 작업: \<(예) FR-1 중 두 곳\>
- Q-2. \<질문\> → 막히는 작업: \<FR 번호\>
