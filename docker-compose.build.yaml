# 레지스트리 대신 지금 소스로 빌드해서 띄운다. 홈서버에서는 쓰지 않는다.
#   docker compose -f docker-compose.yaml -f docker-compose.build.yaml up -d --build
services:
  studywatch:
    build: .
    image: studywatch:local
    pull_policy: build
